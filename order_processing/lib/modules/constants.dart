import 'package:flutter/material.dart';

const Color login_bg = Color(0xFFF9A825);
const Color signup_bg = Color(0xFF000A54);

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 100);